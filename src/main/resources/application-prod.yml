spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/FireFighting
    username: postgres
    password: w.w1w+
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  aop:
    auto: true